/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {MyNewClass} from './my-new-class';

describe('MyNewClass', () => {
  it('should create an instance', () => {
    expect(new MyNewClass()).toBeTruthy();
  });
});
